<Project>
  <PropertyGroup>

    <!-- Assembly information. -->
    <Product>BD.AppCenter.Core</Product>
    <RootNamespace>BD.AppCenter</RootNamespace>
    <AssemblyName>BD.AppCenter</AssemblyName>
    <AssemblyTitle>BD.AppCenter.Core</AssemblyTitle>
  </PropertyGroup>
  <ImportGroup>
  
    <!-- It stops at the first Directory.Build.props, so we need to manually include upward one. -->
    <Import Project="../Directory.Build.props" />
  </ImportGroup>
</Project>
