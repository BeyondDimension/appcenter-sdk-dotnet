<Project>
  <PropertyGroup>

    <!-- Assembly information. -->
    <Product>BD.AppCenter.Crashes</Product>
    <RootNamespace>BD.AppCenter.Crashes</RootNamespace>
    <AssemblyName>BD.AppCenter.Crashes</AssemblyName>
    <AssemblyTitle>BD.AppCenter.Crashes</AssemblyTitle>
  </PropertyGroup>
  <ImportGroup>
  
    <!-- It stops at the first Directory.Build.props, so we need to manually include upward one. -->
    <Import Project="../Directory.Build.props" />
  </ImportGroup>
</Project>

