<Project>
  <PropertyGroup>

    <!-- Assembly information. -->
    <Product>BD.AppCenter.Analytics</Product>
    <RootNamespace>BD.AppCenter.Analytics</RootNamespace>
    <AssemblyName>BD.AppCenter.Analytics</AssemblyName>
    <AssemblyTitle>BD.AppCenter.Analytics</AssemblyTitle>
  </PropertyGroup>
  <ImportGroup>
  
    <!-- It stops at the first Directory.Build.props, so we need to manually include upward one. -->
    <Import Project="../Directory.Build.props" />
  </ImportGroup>
</Project>

