<Project>
  <PropertyGroup>

    <!-- Assembly information. -->
    <Product>BD.AppCenter.Distribute</Product>
    <RootNamespace>BD.AppCenter.Distribute</RootNamespace>
    <AssemblyName>BD.AppCenter.Distribute</AssemblyName>
    <AssemblyTitle>BD.AppCenter.Distribute</AssemblyTitle>
  </PropertyGroup>
  <ImportGroup>
  
    <!-- It stops at the first Directory.Build.props, so we need to manually include upward one. -->
    <Import Project="../Directory.Build.props" />
  </ImportGroup>
</Project>

